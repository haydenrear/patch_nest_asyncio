[project]
name = "patch-asyncio"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "nest-asyncio==1.6.0"
]

[project.scripts]
patch = "main"
